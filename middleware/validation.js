import { booleanLiteral, stringLiteral } from "@babel/types";

// Accepts an array of objects
// Each object has a specified schema

// Example Objects:

{
    name: String,
    required: Boolean,
    type: String (param || query || body),
    
}